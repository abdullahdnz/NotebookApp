@model Note

@{
    ViewData["Title"] = "Edit";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">

<h1>Edit Note</h1>
<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="ID" />

                <div class="form-group">
                    <label asp-for="Title" style="font-weight:900">Name</label>
                    <input type="text" class="form-control" asp-for="Title" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Description" style="font-weight:900">Description</label>
                    <textarea asp-for="Description" rows="8" cols="70"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <br />
                <div hidden class="form-group">
                    <label asp-for="CreateDate" style="font-weight:900">Create Date</label>
                    <input type="text" class="form-control" asp-for="CreateDate" />
                    <span asp-validation-for="CreateDate" class="text-danger"></span>
                </div>

                @if (TempData["msg"] != null)
                {
                    <div class="form-group">
                        @TempData["msg"]
                    </div>
                }

                <div class="form-group pt-3">
                    <button type="submit" class="btn btn-success"><i class="fa-solid fa-floppy-disk" style="padding:5px"></i>Save</button>
                    <a href="/Note/GetAll" class="btn btn-primary"><i class="fa-solid fa-list" style="padding:5px"></i>List</a>
                </div>
            </form>
        </div>
    </div>
</div>